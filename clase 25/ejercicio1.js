let cuadrado = (num) => {
    let resultado = num * num;
    return resultado
}

console.log("El resultado al cuadrado es:",cuadrado(10) )


let cuadrado2 = num => num * num;
console.log ("El cuadrado es:", cuadrado(5));